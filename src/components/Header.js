import React from 'react';
import { StyledHeader } from '../styles/StyledHeader';

const Header = () => (
  <StyledHeader>
    <h1>
      Movie timer
    </h1>
  </StyledHeader>
);

export default Header;
